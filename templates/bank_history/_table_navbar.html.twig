<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item me-1">
					<select class="form-select form-select-sm me-1" {{ stimulus_target('pager', 'sorting') }} {{ stimulus_action('pager', 'parameterChanged') }}>
						<option value="date-asc">Data rosnąco</option>
						<option value="date-desc">Data malejąco</option>
						<option value="category-asc">Kategoria rosnąco</option>
						<option value="category-desc">Kategoria malejąco</option>
						<option value="value-asc">Kwota rosnąco</option>
						<option value="value-desc">Kwota malejąco</option>
					</select>
				</li>
				<li class="nav-item me-1">
					<select class="form-select form-select-sm me-1" {{ stimulus_target('pager', 'category') }} {{ stimulus_action('pager', 'parameterChanged') }}>
						<option value="">Wszystkie</option>
						<option value="brak">Brak</option>
						<option value="darowizna">Darowizna</option>
						<option value="dofinansowanie">Dofinansowanie</option>
						<option value="koszty">Koszty</option>
					</select>
				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle fs-6 pb-1 pt-1" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Filtrowanie po kwocie
					</a>
					<ul class="dropdown-menu">
						<li>
							<div class="pt-1 pb-1 ps-2 pe-2">
								Kwota od
								<input type="number" id="value_from" name="value_from" step="any" class="form-control form-control-sm" {{ stimulus_target('pager', 'valueFrom') }}>
							</div>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<div class="pt-1 pb-1 ps-2 pe-2">
								Kwota do
								<input type="number" id="value_to" name="value_to" step="any" class="form-control form-control-sm" {{ stimulus_target('pager', 'valueTo') }}>

							</div>
						</li>
						<li><hr class="dropdown-divider">
						</li>
						<li>
							<a class="btn btn-primary d-grid ms-2 me-2" href="#" {{ stimulus_action('pager', 'filterClicked') }}>Filtruj</a>
						</li>
					</ul>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle fs-6 pb-1 pt-1" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Filtrowanie po dacie
					</a>
					<ul class="dropdown-menu">
						<li>
							<div class="pt-1 pb-1 ps-2 pe-2">
								Data od
								<input type="date" id="start_date" name="start_date" class="form-control" value="" {{ stimulus_target('pager', 'startDate') }}>
							</div>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<div class="pt-1 pb-1 ps-2 pe-2">
								Data do
								<input type="date" id="end_date" name="end_date" class="form-control" value="" {{ stimulus_target('pager', 'endDate') }}>
							</div>
						</li>
						<li><hr class="dropdown-divider">
						</li>
						<li>
							<a class="btn btn-primary d-grid ms-2 me-2" href="#" {{ stimulus_action('pager', 'filterClicked') }}>Filtruj</a>
						</li>
					</ul>
				</li>
				<li class="nav-item me-1">
					<input class="form-control form-control-sm" type="text" placeholder="Szukaj..." {{ stimulus_target('pager', 'details') }} {{ stimulus_action('pager', 'parameterChanged') }}>
				</li>
				<li class="nav-item me-1">
					<button class="btn btn-sm btn-light" {{ stimulus_target('pager', 'flagged') }} {{ stimulus_action('pager', 'flaggedClicked') }} data-bs-toggle="tooltip" data-bs-title="Pokaż tylko oznaczone">
						<span class="icon ">
							<i class="fa-solid fa-flag"></i>
						</span>
					</button>
				</li>
				<li class="nav-item me-1">
					<span class="d-inline-block" tabindex="0" data-bs-toggle="tooltip" data-bs-title="Wyczyść filtrowanie">
						<button class="btn btn-sm btn-warning" {{ stimulus_target('pager', 'clearFilters') }} {{ stimulus_action('pager', 'clearFiltersClicked') }}>
							<i class="fa-solid fa-eraser"></i>
						</button>
					</span>
				</li>

			</ul>

			<div class="d-flex">
				{% include "bank_history/_pager_controls.html.twig" %}
			</div>
		</div>
	</div>
</nav>
